<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Hotel Results</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Swiper CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"/>

  <style>
    .swiper {
      width: 100%;
      padding-bottom: 20px;
    }

    .swiper-slide img {
      width: 100%;
      height: 120px;
      object-fit: cover;
      border-radius: 8px;
      cursor: pointer;
      transition: transform 0.3s ease;
    }

    .swiper-slide img:hover {
      transform: scale(1.05);
    }

    .swiper-button-prev, .swiper-button-next {
      color: #000;
    }

    .hotel {
      border: 1px solid #ddd;
      padding: 1.5rem;
      border-radius: 10px;
      margin-bottom: 2rem;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }
  </style>
</head>

<body class="container py-5">

  <% hotels.forEach(hotel => { %>
    <div class="hotel">
      <h3><%= hotel.name %></h3>
      <p><%= hotel.description %></p>

      <!-- Swiper Carousel -->
      <div class="swiper mySwiper swiper-<%= hotel.code %>">
        <div class="swiper-wrapper">
          <% hotel.images.forEach((image, index) => { %>
            <div class="swiper-slide">
              <img src="https://photos.hotelbeds.com/giata/<%= image.path %>" alt="Hotel image">
            </div>
          <% }) %>
        </div>
        <div class="swiper-button-next swiper-button-<%= hotel.code %>"></div>
        <div class="swiper-button-prev swiper-button-<%= hotel.code %>"></div>
      </div>

      <h5>Price from: <%= hotel.price %> <%= hotel.currency %></h5>

      <% if (hotel.rooms && hotel.rooms.length > 0) { %>
        <div>
          <% hotel.rooms.forEach(room => { %>
            <div class="room mt-3">
              <strong><%= room.name %></strong>
              <% if (room.rates) { %>
                <% room.rates.forEach(rate => { %>
                  <div>Price: <%= rate.net %> <%= hotel.currency %> - <%= rate.boardName %></div>
                <% }) %>
              <% } else { %>
                <div>No rate info available</div>
              <% } %>
            </div>
          <% }) %>
        </div>
      <% } %>

      <% if (hotel.facilities && hotel.facilities.length > 0) { %>
        <div class="facilities mt-3">
          <strong>Facilities:</strong>
          <ul>
            <% hotel.facilities.forEach(fac => { %>
              <li><%= fac.description || fac.description?.content || "Unnamed facility" %></li>
            <% }) %>
          </ul>
        </div>
      <% } %>
    </div>
  <% }) %>

  <!-- JS Dependencies -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

  <!-- Swiper Initialization -->
  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const swipers = document.querySelectorAll(".swiper");
      swipers.forEach((swiperEl) => {
        const hotelCode = swiperEl.classList[2]?.split("-")[1]; // e.g. 'swiper-26852'
        new Swiper(".swiper-" + hotelCode, {
          slidesPerView: 3,
          spaceBetween: 10,
          navigation: {
            nextEl: ".swiper-button-" + hotelCode + ".swiper-button-next",
            prevEl: ".swiper-button-" + hotelCode + ".swiper-button-prev",
          },
          breakpoints: {
            768: { slidesPerView: 4 },
            1024: { slidesPerView: 5 }
          }
        });
      });
    });
  </script>

</body>
</html>
